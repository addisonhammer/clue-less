<!doctype html>
<div style="text-align:center">
<title>Clue-Less</title>
<h1>Who done it?</h1>
    <p>Your Guess was {{ suspect }} in the {{ room }} with the {{ weapon }}...</p>
    <p>Your Accusation was
      {% if result %}
      Correct!
      {% else %}
      Incorrect!
      {% endif %}
    </p>

    <form method="post">
      <input type="submit" value="Make an Accusation">
      <input type="hidden" id="player" name="player" value={{player}}>
    </form>

    <form method="post">
      <input type="submit" value="Move">
      <input type="hidden" id="player" name="player" value={{player}}>
    </form>
</div>
