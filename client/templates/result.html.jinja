<!doctype html>
<title>Clue-Less</title>

<h1>Your Guess was {{ accused }} in the {{ room }} with the {{ weapon }}...</h1>
(sent by: {{player}})
<h1>Your Accusation was 
  {% if valid %}
  Correct!
  {% else %}
  Incorrect!
  {% endif %}
</h1>

{# <form method="post">
  <input type="submit" value="Guess Again">
  <input type="hidden" id="player" name="player" value={{player}}>
</form> #}