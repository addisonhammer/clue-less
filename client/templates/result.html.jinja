<!doctype html>
<div style="text-align:center">
<title>Clue-Less</title>
<h1>Who done it?</h1>
    <p>Your suggestion was {{ suspect }} in the {{ room }} with the {{ weapon }}...</p>
    <p>Your Accusation was
      {% if result %}
      Correct!
      {% else %}
      Incorrect!
      {% endif %}
    </p>

    <form method="post">
      <input name="button" type="submit" value="Make an Accusation">
      <input name="button" type="submit" value="Move">
    </form>
</div>
